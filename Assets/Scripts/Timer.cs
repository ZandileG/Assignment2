using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Timer : MonoBehaviour
{
   //The timer needs to run for 1-2min 
   //The timer must be different for each level depending on its difficulty
   //The timer must continue even when the player is completing a puzzle
   //When the timer runs out the player must restart the level
}
